# Frontend (Vite) — required.
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-static-api-key

# Frontend — Vite proxies /detect, /diagnose, /scan-video to the backend.
# No backend URL needed here; see vite.config.js.
